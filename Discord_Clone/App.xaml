<Application x:Class="Discord_Clone.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Discord_Clone"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <FontFamily x:Key="ggsansmonoBold">./Fonts/ggsansmono-Bold.ttf#ggsansmono</FontFamily>
        <FontFamily x:Key="ggsansmonoNormal">./Fonts/ggsansmono-Normal.ttf#ggsansmono</FontFamily>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#393B41"/>
            <Setter Property="BorderBrush" Value="#42454C"/>
            <Setter Property="Foreground" Value="#FFFFFF"
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
